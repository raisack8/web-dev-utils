'use client'; import { useState } from 'react'; interface
{{pascalCase name}}Props { children?: React.ReactNode; } export default function
{{pascalCase name}}({ children }:
{{pascalCase name}}Props) { return (
<div>
  {children}
</div>
); }